#include <iostream>
class operation
{   private: int date;
             int sum;
             char holder;
             operation();
};
operation::operation(){
					char date = 0;
                     int sum = 0;
                     char holder = 0;
                    };
/*person::person ()
* {
* if(name)
* 	delete[] name;
* }
* */
class person
{
    private:*char lastname;
            *char name;
    public person(const char *lastname,const char *name);
                {
                    name =new char [strlen(name)+1];
                    lastname
                    strcpy(name,1);
                    }
  ~person ()
{if(name)
	delete[] name;
	 }
};

class bank
{
    int date;
    int sum
    person p;
    operation h[4]; 
    bank();
};
bank::bank{int date;
            operation h[4] =={0};
            person p = 0;
            };
     
int bank::replenish account()
{
    int amount_in;
    cout<<"amount"<<endl;
    cin>>amount_in>>;
    sum+=amount_in;
    }
int bank::takeout()
{
    int amount;
    cout<<"amount="<<endl;
    cin>>amount>>;
    if (sum<amount )
        {perror("amount is over high");return -1}
    else if(sum >= amount)
    }

int bank::sum_of_account()
{
    cout<<"sum of account is"<<sum<<endl; 
    }
    
int last_4_oper()
{
    
    }
int main()
{
	person p();
    cin>>"">>
    cout<< ""<<   
    return 0;
}

